#ifndef INPUT_H
	#define INPUT_H

#include <iostream>
#include <sstream>
#include <string>

void run();
int checkNum(string str);
int checkValidCommand(string str);
void doStuff(string str, char command);

#endif